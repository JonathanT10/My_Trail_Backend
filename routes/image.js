// const {Image} = require('../models/image');
// const express = require('express');
// const router = express.Router();

// router.get('/:id', async (req, res) => {
//     try{
//         const image = await Image.find(userId: req.params.id);
//     }
// })